{
  "name": "default",
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "myuser",
  "password": "mypass",
  "database": "mydb",
  "synchronize": false,
  "logging": true,
  "entities": ["src/entity/*.ts"],
  "migrations": ["migration/*.ts"],
  "cli": {
    "migrationsDir": "migration"
  }
}
